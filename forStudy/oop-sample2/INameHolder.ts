// 次のinterfaceに従って、文字列型のプロパティ_nameを持つクラスNameHolderを作成してください。このクラスは、Getterを使ってプロパティを取得し、Setterを使ってプロパティに新しい値を設定できるようにしてください。ただし、新しい値は大文字で始まる必要があります。そうでなければ、値を設定できません。
interface INameHolder {
    name: string;
}